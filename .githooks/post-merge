#!/usr/bin/env bash
# Auto-installation après chaque git pull / git merge.
ROOT="$(git rev-parse --show-toplevel)"
echo "[post-merge] Réinstallation des dépendances melko-tfpb-automation…"
bash "$ROOT/go.sh" install
